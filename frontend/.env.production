# Configuración usada por `npm run build` (modo production).
# No contiene secretos: son valores públicos que quedan embebidos en el bundle.

VITE_API_URL=https://rezon.ar/api

# Propiedad de Google Analytics. Sin esto no se mide nada: es lo que pasaba
# antes, cuando el valor vivía en duro en el index.html y acá faltaba.
VITE_GA_MEASUREMENT_ID=G-3W7JFPFSBL
